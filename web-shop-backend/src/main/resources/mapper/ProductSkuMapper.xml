<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.javaweb.webshopbackend.mapper.ProductSkuMapper">

    <resultMap id="BaseResultMap" type="org.javaweb.webshopbackend.pojo.entity.ProductSku">
        <id column="id" property="id"/>
        <result column="product_id" property="productId"/>
        <result column="sku_code" property="skuCode"/>
        <result column="sku_name" property="skuName"/>
        <result column="attributes" property="attributes"/>
        <result column="price" property="price"/>
        <result column="original_price" property="originalPrice"/>
        <result column="stock" property="stock"/>
        <result column="sales" property="sales"/>
        <result column="image" property="image"/>
        <result column="status" property="status"/>
        <result column="deleted" property="deleted"/>
        <result column="created_time" property="createdTime"/>
        <result column="updated_time" property="updatedTime"/>
    </resultMap>

    <select id="selectByProductId" resultMap="BaseResultMap">
        SELECT * FROM product_sku
        WHERE product_id = #{productId} AND deleted = 0
        ORDER BY id ASC
    </select>

    <update id="updateStock">
        UPDATE product_sku
        SET stock = stock + #{quantity}
        WHERE id = #{skuId}
    </update>

</mapper>