<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.javaweb.webshopbackend.mapper.UserMapper">

    <!-- 基础结果映射 -->
    <resultMap id="BaseResultMap" type="org.javaweb.webshopbackend.pojo.entity.User">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="nickname" property="nickname" />
        <result column="email" property="email" />
        <result column="phone" property="phone" />
        <result column="avatar" property="avatar" />
        <result column="role" property="role" />
        <result column="status" property="status" />
        <result column="last_login_time" property="lastLoginTime" />
        <result column="created_time" property="createdTime" />
        <result column="updated_time" property="updatedTime" />
        <result column="deleted" property="deleted" />
    </resultMap>

    <!-- 根据用户名查询用户（忽略逻辑删除） -->
    <select id="selectByUsernameIgnoreDeleted" resultMap="BaseResultMap">
        SELECT id, username, password, nickname, email, phone, avatar, role, status,
               last_login_time, created_time, updated_time, deleted
        FROM user
        WHERE username = #{username}
        LIMIT 1
    </select>

    <!-- 根据手机号查询用户（忽略逻辑删除） -->
    <select id="selectByPhoneIgnoreDeleted" resultMap="BaseResultMap">
        SELECT id, username, password, nickname, email, phone, avatar, role, status,
               last_login_time, created_time, updated_time, deleted
        FROM user
        WHERE phone = #{phone}
        LIMIT 1
    </select>

    <!-- 更新最后登录时间 -->
    <update id="updateLastLoginTime">
        UPDATE user
        SET last_login_time = NOW()
        WHERE id = #{userId}
          AND deleted = 0
    </update>

</mapper>

